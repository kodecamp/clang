#include <stdbool.h>

extern bool str_is_equal(const char* str1,const char* str2);

extern int str_len(const char *str);

extern bool str_cmp(const char *str1,const char *str2);

extern char* to_uppercase(const char *str);

